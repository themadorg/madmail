"""Madmail service + config paths shared by SSH-based scenarios."""

from __future__ import annotations

MAD_SERVICE = "madmail.service"
MAD_CONFIG_PATHS = ("/etc/madmail/madmail.conf", "/etc/maddy/maddy.conf")